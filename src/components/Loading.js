import React from "react";

const Loading = () => {
  return (
        <img
      style={{ width: "300px" }}
      className="m-auto mt-8"
      src="/loading.svg"
      alt="cargando"
    />
  );
};

export default Loading;
